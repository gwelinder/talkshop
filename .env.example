# Supabase Configuration (Required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Tavus API Configuration (Required for AI video hosts)
VITE_TAVUS_API_KEY=your_tavus_api_key_here

# ElevenLabs API Configuration (Optional - for voice features)
VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key

# RevenueCat Configuration (Optional - for monetization)
VITE_REVENUECAT_API_KEY=your_revenuecat_api_key

# Optional: Custom webhook URL (defaults to built-in webhook)
VITE_WEBHOOK_URL=

# Google OAuth Configuration (Handled by Supabase)
# Configure in Supabase Dashboard > Authentication > Providers > Google
# Use these URLs for your OAuth client:
# Authorized JavaScript origins: https://timely-piroshki-b9d6cc.netlify.app
# Authorized redirect URIs: https://timely-piroshki-b9d6cc.netlify.app?auth=callback

# SECURITY NOTE:
# Never commit actual API keys to your repository!
# Set these as environment variables in your deployment platform:
# - Netlify: Site settings > Environment variables
# - Vercel: Project settings > Environment Variables
# - Railway: Variables tab